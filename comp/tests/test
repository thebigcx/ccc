#include "tests/stdio"

/*

Hello

    /*
        nested comments!
    */
*/

fn test(x: int32)
{

}

fn main(argc: int32, argv: int8**)
{
    var x: int64;
    x = 10;

    while (x > 0)
    {
        x = x - 1;
        test();
        if (x == 2)
        {
            goto end;
        }
    }

label end:
}