#include "tests/stdio.h"

fn func(x: int32) -> int32
{
    return 40 * x;
}

fn public main(argc: int32, argv: int8**) -> int32
{
    var x = 10;
    var y = 100;
    var z = 20;

    printf("%d\n", func(10));
}