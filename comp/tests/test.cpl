#include "tests/stdio.h"
#include "tests/stdio.h"

/*
Hello
*/

#define PRINT

typedef Integer32 = fn* (int32, int8**) -> int32;

fn public main(argc: int32, argv: int8**) -> int32
{
    var x: Integer32 = &main;

    for (var i = 0; i < argc; i++)
    {
        printf("%s\n", argv[i]);
    }

    return x(argc, argv);
}