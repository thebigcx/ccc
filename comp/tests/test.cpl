#include "tests/stdio.h"

#define FOO '.'

fn public main(argc: int32, argv: int8**) -> int32
{
    var x = 10;
    var y = &x;
    (*y)++;
    printf("%d\n", *y);
    if ((uint64)argv && argv[0][0] == FOO)
    {
        printf("%d\n", -1 - 100);
    }
    return 0;
}