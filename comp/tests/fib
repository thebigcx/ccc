#include "tests/stdio"

fn main()
{
    var x, y, z: int32;

    x = 1;
    y = 1;
    z = 0;

    while (z < 100)
    {
        z = x + y;
        x = y;
        y = z;
    }

    var narrow: int8;
    narrow = 10;

    var wide: int64;
    wide = (int64)narrow;
}